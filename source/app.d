import std.stdio;

import mood.node;
import mood.parser;
import mood.compiler;
import mood.server;

void main()
{
	// enum tokens = tokenizeDHTML(import("test.html"));
	// static foreach(tok; tokens)
	// {
	// 	pragma(msg, "-----BEGIN NODE-----");
	// 	pragma(msg, tok);
	// 	pragma(msg, "-----END NODE-----");
	// }
	// enum nodes = parseDHTML(tokens);
	// enum doc = compile!(nodes)();
	// enum test = compile!"test.html";
	// writeln(serve(test));
	// enum foo = compile!"foo.html";
	// writeln(serve(foo));
}